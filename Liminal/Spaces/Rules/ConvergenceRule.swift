/// Space 7: all prior rules at low intensity simultaneously.
/// Stub — real implementation in Phase 4.
struct ConvergenceRule: SpaceRule {
    func evaluate(playerState: PlayerState) -> RuleOutput { .empty }
}
